version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:16
    working_directory: ~/app
    steps:
      - checkout
      - run: npm install
      - run: npm install node-fetch
       # Print dependencies
      - run:
          name: Print Dependencies
          command: npm ls
      - run: npm run test
      


      